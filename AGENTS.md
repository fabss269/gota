# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Before doing anything else in this repo

Read `docs/ESTADO_PROYECTO.md` first. It has the current architecture decisions (this
app targets **web**, not native — see why there), the real-cause fixes for the
web-compat bugs already solved (so you don't re-diagnose them from scratch), and an
honest list of what's verified vs. still open. `specs/00-auditoria-diseno.md` and
`specs/01`-`09` cover product/design decisions per feature; `docs/ESTADO_PROYECTO.md`
covers the technical/infra state.
